# Visual System Overview

## Complete System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                      USER (Browser)                          │
│                   http://localhost:3000                      │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                  NEXT.JS FRONTEND                            │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ Header: "Enterprise AI Knowledge Assistant"          │  │
│  │ Status: ● Connected                                  │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ Chat Messages (scrollable)                           │  │
│  │                                                       │  │
│  │  👤 User: What is the budget policy?                 │  │
│  │                                                       │  │
│  │  🤖 AI: Based on the AWS budget documents...         │  │
│  │     📄 Sources: aws_budget_policy.md                 │  │
│  │     Confidence: [High]                               │  │
│  │                                                       │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ 📎  [Type your question...]              [Send ➤]   │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  Components:                                                 │
│  • ChatMessage.tsx • ChatInput.tsx                          │
│  • SourceBadge.tsx • ConfidenceBadge.tsx                   │
│  • LoadingIndicator.tsx                                     │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ HTTP POST /api/chat
                            │ HTTP POST /api/upload
                            ▼
┌─────────────────────────────────────────────────────────────┐
│               NEXT.JS API ROUTES (Proxy)                     │
│                                                              │
│  /api/chat    → Forward to http://localhost:8000/chat       │
│  /api/upload  → Forward to http://localhost:8000/upload     │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│             FLASK API SERVER (api_server.py)                 │
│                  Port 8000                                   │
│                                                              │
│  Endpoints:                                                  │
│  • POST /chat     → Process questions                       │
│  • POST /upload   → Upload docs + re-index                  │
│  • GET /health    → Health check                            │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                  INTENT ROUTER (Agent)                       │
│                                                              │
│  Question Analysis:                                          │
│  "Hello" → ANSWER_DIRECTLY (no retrieval)                   │
│  "Budget policy?" → RETRIEVE_AND_ANSWER (use RAG)           │
│  "Who won Super Bowl?" → REFUSE (out of scope)              │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                    RAG PIPELINE                              │
│                                                              │
│  1. Query Embedding (Google Gemini)                         │
│     "What is budget policy?" → [0.2, 0.8, ...]              │
│                                                              │
│  2. Vector Search (ChromaDB)                                │
│     Find top 3 most similar document chunks                 │
│                                                              │
│  3. LLM Generation (Google Gemini Pro)                      │
│     Context + Question → Answer                             │
│                                                              │
│  4. Source Extraction                                        │
│     Return document filenames                               │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│               ANSWER VERIFIER (Agent)                        │
│                                                              │
│  Validation:                                                 │
│  • Check all claims against sources                         │
│  • Detect hallucinations                                    │
│  • If invalid → "I don't know"                              │
│  • If valid → Return answer                                 │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│              RESPONSE FORMATTING                             │
│                                                              │
│  {                                                           │
│    "answer": "Based on the AWS budget policy...",           │
│    "sources": ["aws_budget_policy.md"],                     │
│    "confidence": "High"                                      │
│  }                                                           │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
                   [Back to Frontend]
```

---

## File Upload Flow

```
1. User clicks 📎 icon
   │
   ▼
2. File picker opens → User selects file.md
   │
   ▼
3. Frontend validates (client-side)
   • Check extension (.md, .pdf, .txt)
   • Check size (< 16MB)
   │
   ▼
4. POST /api/upload (FormData)
   │
   ▼
5. Next.js API route → Forward to Flask
   │
   ▼
6. Flask backend receives file
   │
   ▼
7. Save to data/raw/file.md
   │
   ▼
8. Delete old vectorstore/
   │
   ▼
9. Re-ingest ALL documents
   ├─ Load files from data/raw/
   ├─ Chunk into 1000-token segments
   ├─ Generate embeddings
   └─ Create new vectorstore/
   │
   ▼
10. Re-initialize QA chain
   │
   ▼
11. Return success response
   │
   ▼
12. Frontend shows ✅ Success notification
```

---

## Data Storage

```
data/
├── raw/                      ← Document Storage
│   ├── aws_budget_policy.md
│   ├── employee_handbook.md
│   └── [user_uploaded].md
│
└── vectorstore/              ← Vector Database (ChromaDB)
    ├── chroma.sqlite3       ← Embeddings database
    └── [metadata files]
```

---

## Technology Stack Visual

```
┌─────────────────────────────────────────┐
│            FRONTEND                      │
├─────────────────────────────────────────┤
│ Next.js 14     TypeScript               │
│ React 18       Tailwind CSS             │
│ API Routes     Fetch API                │
└─────────────────────────────────────────┘
                  ↕️
┌─────────────────────────────────────────┐
│             BACKEND                      │
├─────────────────────────────────────────┤
│ Flask 3.0      Flask-CORS               │
│ LangChain      Google Gemini            │
│ ChromaDB       Python 3.9+              │
└─────────────────────────────────────────┘
```

---

## Request Timeline

```
User sends message
↓ 0ms
Frontend: Create user message bubble
↓ 10ms
Frontend: POST /api/chat
↓ 20ms
Next.js API: Proxy to backend
↓ 30ms
Flask: Receive request
↓ 50ms
Intent Router: Analyze intent (100ms)
↓ 150ms
RAG: Query vectorstore (200ms)
↓ 350ms
RAG: Call Google Gemini (2000ms)
↓ 2350ms
Answer Verifier: Validate (100ms)
↓ 2450ms
Flask: Format response
↓ 2460ms
Next.js API: Return to frontend
↓ 2470ms
Frontend: Create AI message bubble
↓ 2480ms
Frontend: Display answer + sources
↓ 2500ms
User sees complete response

Total: ~2.5 seconds (typical)
```

---

## Color-Coded Status Indicators

```
● Green   = Connected, High Confidence, Success
● Yellow  = Medium Confidence, Processing
● Orange  = Low Confidence, Warning
● Red     = Disconnected, Error, Failed
● Blue    = User message, Active action
● Gray    = AI message, Neutral state
```

---

## Component Hierarchy

```
App (page.tsx)
│
├─ Header
│  ├─ Title
│  └─ Status Indicator
│
├─ Notification Bar
│  ├─ Error Alert
│  └─ Success Alert
│
├─ Chat Container
│  ├─ Empty State (if no messages)
│  │  ├─ Welcome Icon
│  │  ├─ Welcome Text
│  │  └─ Suggested Questions
│  │
│  ├─ Message List
│  │  ├─ ChatMessage (User)
│  │  ├─ ChatMessage (Assistant)
│  │  │  ├─ Answer Text
│  │  │  ├─ Sources (SourceBadge × N)
│  │  │  └─ Confidence (ConfidenceBadge)
│  │  └─ LoadingIndicator
│  │
│  └─ Scroll Anchor
│
└─ ChatInput
   ├─ Upload Button (📎)
   ├─ Text Input
   └─ Send Button (➤)
```

---

## Error Handling Flow

```
                    ┌─────────────────┐
                    │  User Action    │
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │  Try Request    │
                    └────────┬────────┘
                             │
                    ┌────────┴────────┐
                    │                 │
              Success               Error
                    │                 │
                    ▼                 ▼
           ┌─────────────┐   ┌─────────────┐
           │Show Response│   │Catch Error  │
           └─────────────┘   └──────┬──────┘
                                     │
                            ┌────────┴────────┐
                            │                 │
                      Network            Backend
                       Error              Error
                            │                 │
                            ▼                 ▼
                    Show retry          Show error
                      message            message
```

---

## Responsive Breakpoints Visual

```
Mobile (< 640px)
┌──────────┐
│  Header  │
├──────────┤
│          │
│ Messages │
│          │
├──────────┤
│  Input   │
└──────────┘

Tablet (640px - 1024px)
┌────────────────┐
│     Header     │
├────────────────┤
│                │
│    Messages    │
│                │
├────────────────┤
│     Input      │
└────────────────┘

Desktop (> 1024px)
┌────────────────────────────┐
│          Header            │
├────────────────────────────┤
│                            │
│        Messages            │
│    (max-width: 896px)      │
│                            │
├────────────────────────────┤
│          Input             │
└────────────────────────────┘
```

---

**This visual guide provides a complete mental model of the system.** 🎨
